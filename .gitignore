node_modules/
dist/
coverage/
.vercel/
.vite/

# Playwright
test-results/
playwright-report/
blob-report/
.playwright/

# Environment — never commit
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# Logs
*.log
npm-debug.log*

# SWEAI Builder is a submodule at .ai-engineering; a product-owned copy is invalid.
.bootstrap/

# Gate evidence is a generated artifact, never a tracked file. Committing it
# would dirty the worktree it describes and change the head it is stamped with.
evidence.json
evidence-report.md
review-evidence/

# Produced by `npm run smoke:prod`; the record lives in docs/quality/gates.md.
smoke-results.json
test-results/
